Сейчас: патч картинки профиля + интеграция обновленных свойств картинок в элементы фронтенда
Добавить, общую картинку для новорегов / или красочный шаблон аватара типа "A" на фоне если у чела ник Avatar какой-нить

------
1) Проблема с выходом из аккаунта - решить
2) Отсутствует перенаправление на главную страницу при тухлом токене
3) При регистрации надо добавить логику указания ника и проверку на его доступность

4) Допиши формат для диапозона игровых дат
5) При регистрации должно схватываться время пользователя

---На будущее---
нб1) Полный перенос генерации ссылок на бэкенд NestJS
Фронтенду вообще не требуется знать про imgproxy и названия его пресетов. Сервер NestJS при отдаче DTO пользователя или сущности сам формирует готовый подписанный URL под нужный контекст. Фронтенд получает из API готовую строку и рендерит её в атрибуте src. Это полностью снимает с фронтенда задачу управления медиа-параметрами.

Общий пакет констант в монорепозитории
Если фронтенд и бэкенд находятся в одном репозитории, список пресетов объявляется один раз массивом TypeScript.
На основе массива через оператор typeof формируются типы фронтенда, а вспомогательная функция перед стартом контейнера собирает из этого же массива итоговую строку параметров для Docker.

Единый JSON-файл с автогенерацией
Создается один файл конфигурации с описанием всех пресетов.
Скрипт сборки перед запуском приложения читает данный файл, автоматически генерирует d.ts файл с типами для Nuxt и прописывает переменные окружения.

Самым чистым архитектурным решением является генерация ссылок на бэкенде. Фронтенд должен отображать данные, а не знать детали внутренней инфраструктуры оптимизации картинок.
